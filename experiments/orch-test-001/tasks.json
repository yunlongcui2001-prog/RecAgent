{
  "tasks": [
    {
      "id": "t1",
      "op": "create",
      "path": "experiments/orch-test-001/code/calc.py",
      "spec": "1. 创建函数 add(a: int, b: int) -> int，返回 a + b。\n2. 创建函数 multiply(a: int, b: int) -> int，返回 a * b。",
      "test_cmd": "python -m pytest experiments/orch-test-001/code/test_calc.py -v"
    }
  ],
  "success_criteria": {
    "smoke_cmd": "python -c \"import sys; sys.path.insert(0,'experiments/orch-test-001/code'); from calc import add,multiply; assert add(1,2)==3; assert multiply(3,4)==12; print('smoke OK')\"",
    "asserts": ["exit code == 0", "stdout 含 'smoke OK'"]
  }
}
